---
tipo: "texto"
autor: "Daniel Villegas"
titulo: "Recuperación & Reexplotación. Acerca de las canciones de trabajo"
---

import Layout from "../../layouts/Layout.astro";

<Layout
  titulo={frontmatter.titulo}
  tipo={frontmatter.tipo}
  autor={frontmatter.autor}
>
  <h1>{frontmatter.titulo}</h1>
  <p>{frontmatter.autor}</p>
</Layout>
